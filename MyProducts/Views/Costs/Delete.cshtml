@model MyProducts.Models.Costs
@{
    ViewBag.Title = "Удаление товара из прайс листа";
}

<div class="w-50">
    <div>
        <p>Вы действительно хотите удалить товар из таблицы реализации с кодом "<b>@Model.Id</b>"?</p>
    </div>
    <div>
        <form method="post">
            <div class="form-group">
                <input type="submit" class="form-control" value="Удалить" />
            </div>
        </form>
    </div>
</div>